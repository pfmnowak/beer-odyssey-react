export const ITEMS_PER_PAGE = 12;
export const TOTAL_COUNT = 325; // For server-side pagination we need to set this manually since the API doesn't return this value
export const API_URL = 'https://api.punkapi.com/v2/beers';
